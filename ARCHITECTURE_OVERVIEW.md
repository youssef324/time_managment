# Authentication System - Visual Overview

## 🎯 User Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    APP STARTS                               │
│              (initialRoute: '/login')                       │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────┐
        │      LOGIN VIEW          │
        │                          │
        │  • Email Input           │
        │  • Password Input        │
        │  • Sign In Button        │
        │  • Create Account Link   │
        │  • Forgot Password Link  │
        └──────────┬───────────────┘
                   │
        ┌──────────┴───────────┐
        │                      │
        ▼                      ▼
    [Valid]              [Invalid]
        │                      │
        │                      ├─────────────────┐
        │                      │                 │
        │                      ▼                 ▼
        │              [Show Error]      [Clear & Retry]
        │                      │                 │
        │                      └─────────────────┘
        │
        ▼
  ┌──────────────────────┐
  │   AuthService.login  │
  │   1. Validate Email  │
  │   2. Check Password  │
  │   3. Set Current User│
  │   4. Return Response │
  └──────────┬───────────┘
             │
             ▼
    ┌─────────────────┐
    │  SUCCESS        │
    │ Navigate to     │
    │   /home         │
    │ Display Tasks   │
    └─────────────────┘


            SIGN UP FLOW

    ┌─────────────────────────┐
    │  SIGN UP VIEW           │
    │                         │
    │  • Name Input           │
    │  • Email Input          │
    │  • Password Input       │
    │  • Confirm Password     │
    │  • Sign Up Button       │
    │  • Back to Login Link   │
    └────────┬────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │  AuthService.signUp      │
    │  1. Validate All Fields  │
    │  2. Check Email Unique   │
    │  3. Create New User      │
    │  4. Auto-Login User      │
    │  5. Return Response      │
    └────────┬─────────────────┘
             │
             ▼
    ┌─────────────────┐
    │  SUCCESS        │
    │ Navigate to     │
    │   /home         │
    │ Display Tasks   │
    └─────────────────┘
```

## 📦 System Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              LOGIN & SIGN-UP VIEWS                   │  │
│  │  (Handles UI, User Input, Error Display)            │  │
│  └──────────────────┬──────────────────────────────────┘  │
└─────────────────────┼───────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                 STATE MANAGEMENT LAYER                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │            AuthProvider (ChangeNotifier)             │  │
│  │  • Manages loading state                             │  │
│  │  • Stores error messages                             │  │
│  │  • Tracks current user                               │  │
│  └──────────────────┬──────────────────────────────────┘  │
└─────────────────────┼───────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                   BUSINESS LOGIC LAYER                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           AuthService (Core Logic)                   │  │
│  │  • Validation                                        │  │
│  │  • User lookup                                       │  │
│  │  • Account creation                                  │  │
│  │  • Session management                               │  │
│  └──────────────────┬──────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │          ValidationUtils (Helpers)                   │  │
│  │  • Email validation                                  │  │
│  │  • Password validation                               │  │
│  │  • Input sanitization                                │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────┬───────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                    DATA LAYER                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │          In-Memory Storage (Demo)                    │  │
│  │  • User List: [User, User, ...]                      │  │
│  │  • Current User: User?                               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  (Future: Connect to Firebase, API, SQLite, etc.)          │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

```
USER INPUT
    │
    ▼
┌─────────────────┐
│  LoginView      │  • Collects email & password
│  SignUpView     │  • Shows loading state
└────────┬────────┘  • Displays errors
         │
         ▼
┌─────────────────────────┐
│  Input Validation       │  • Checks format
│  (ValidationUtils)      │  • Basic validation
└────────┬────────────────┘
         │
         ▼
┌──────────────────────────┐
│  AuthService             │  • Email validation
│  - login()               │  • Password check
│  - signUp()              │  • User lookup
│  - logout()              │  • Account creation
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│  In-Memory Database      │  • Search users
│  _users list             │  • Create user
│  _currentUser            │  • Update session
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│  AuthResponse            │  • Success/Failure
│  - isSuccess: bool       │  • Message
│  - message: String       │  • User ID
│  - userId: String?       │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│  AuthProvider            │  • Update state
│  - currentUser           │  • Notify listeners
│  - isLoading             │  • Clear errors
│  - errorMessage          │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│  View Updates            │  • Rebuild UI
│  - Show/hide loading     │  • Display message
│  - Enable/disable fields │  • Navigate
│  - Show error            │
└──────────────────────────┘
```

## 🔐 Validation Pipeline

```
INPUT: "user@example.com"
   │
   ▼
TRIM & SANITIZE: "user@example.com"
   │
   ▼
CHECK EMAIL FORMAT
   │
   ├─ NO "@"? ──────────────────▶ ❌ Invalid Format
   │
   ├─ NO "."? ──────────────────▶ ❌ Invalid Format
   │
   └─ MATCHES REGEX? 
      │
      ├─ NO ──────────────────▶ ❌ Invalid Format
      │
      └─ YES
         │
         ▼
      CHECK UNIQUENESS (Sign-up only)
         │
         ├─ EXISTS? ────────────▶ ❌ Already Registered
         │
         └─ NOT EXISTS?
            │
            ▼
         ✅ VALID EMAIL
```

## 📊 State Management Flow

```
┌────────────────────────────────────────────────────────────┐
│              AuthProvider State Transitions                │
└────────────────────────────────────────────────────────────┘

[Initial State]
├─ currentUser: null
├─ isLoading: false
├─ errorMessage: null
└─ isLoggedIn: false


     [User Clicks Login Button]
            │
            ▼
     [Set isLoading: true]
     [Clear errorMessage]
            │
            ▼
     [Call AuthService.login()]
            │
     ┌──────┴──────┐
     │             │
     ▼             ▼
  SUCCESS      FAILURE
     │             │
     ├─ Set user   ├─ Set errorMessage
     ├─ Set        ├─ Keep user: null
     │  isLoading  │
     │  : false    ├─ Set isLoading: false
     │             │
     ▼             ▼
  [LOGGED IN]   [ERROR STATE]


     [User Clicks Logout Button]
            │
            ▼
     [Set isLoading: true]
            │
            ▼
     [Call AuthService.logout()]
            │
            ▼
     [Set user: null]
     [Set isLoading: false]
     [Set errorMessage: null]
            │
            ▼
     [LOGGED OUT]
```

## 🎨 UI Component Hierarchy

```
MyApp
│
├─ MaterialApp
│  │
│  ├─ initialRoute: '/login'
│  │
│  └─ onGenerateRoute: AppRoutes.generateRoute
│     │
│     ├─ /login
│     │  │
│     │  └─ LoginView (StatefulWidget)
│     │     │
│     │     ├─ AppBar
│     │     │  └─ Title
│     │     │
│     │     ├─ Body
│     │     │  ├─ CustomTextField (Email)
│     │     │  ├─ CustomTextField (Password)
│     │     │  ├─ CustomButton (Sign In)
│     │     │  ├─ CustomButton (Create Account)
│     │     │  └─ Forgot Password Link
│     │     │
│     │     └─ Error Display
│     │
│     ├─ /signup
│     │  │
│     │  └─ SignUpView (StatefulWidget)
│     │     │
│     │     ├─ AppBar (with back button)
│     │     │
│     │     ├─ Body
│     │     │  ├─ CustomTextField (Name)
│     │     │  ├─ CustomTextField (Email)
│     │     │  ├─ CustomTextField (Password)
│     │     │  ├─ CustomTextField (Confirm)
│     │     │  ├─ CustomButton (Sign Up)
│     │     │  └─ Sign In Link
│     │     │
│     │     └─ Error Display
│     │
│     ├─ /home
│     │  └─ HomeView
│     │
│     └─ /task_details
│        └─ TaskDetailsView
│
└─ Scaffold
   ├─ AppBar
   ├─ Body
   └─ FloatingActionButton
```

## 💾 Data Model Relationships

```
┌──────────────────────────────────────────────┐
│              User (Model)                    │
├──────────────────────────────────────────────┤
│ - id: String (unique)                        │
│ - email: String (unique)                     │
│ - name: String                               │
│ - password: String (demo only - hash in prod)│
│ - createdAt: DateTime                        │
└──────────────────────────────────────────────┘
           │
           │ Used by
           ▼
┌──────────────────────────────────────────────┐
│         AuthService (Static)                 │
├──────────────────────────────────────────────┤
│ - _users: List<User>                         │
│ - _currentUser: User?                        │
│                                              │
│ Methods:                                     │
│ + login(email, password): AuthResponse       │
│ + signUp(...): AuthResponse                  │
│ + logout()                                   │
│ + getCurrentUser(): User?                    │
└──────────────────────────────────────────────┘
           │
           │ Creates/Uses
           ▼
┌──────────────────────────────────────────────┐
│      AuthResponse (Model)                    │
├──────────────────────────────────────────────┤
│ - isSuccess: bool                            │
│ - message: String                            │
│ - userId: String?                            │
└──────────────────────────────────────────────┘
           │
           │ Consumed by
           ▼
┌──────────────────────────────────────────────┐
│      AuthProvider (ChangeNotifier)           │
├──────────────────────────────────────────────┤
│ - _currentUser: User?                        │
│ - _isLoading: bool                           │
│ - _errorMessage: String?                     │
│                                              │
│ Methods:                                     │
│ + login(...)                                 │
│ + signUp(...)                                │
│ + logout()                                   │
│ + getters                                    │
└──────────────────────────────────────────────┘
           │
           │ Observes
           ▼
┌──────────────────────────────────────────────┐
│    LoginView / SignUpView (UI Layer)        │
├──────────────────────────────────────────────┤
│ Displays user data and state updates         │
└──────────────────────────────────────────────┘
```

## 🔌 Integration Points

```
┌─────────────────────────────────────────────────────────┐
│              EXTERNAL INTEGRATIONS                      │
└─────────────────────────────────────────────────────────┘

FUTURE BACKEND
    ▲
    │
    └─── Replace AuthService methods with:
         • HTTP requests (http package)
         • Firebase Auth
         • Supabase
         • Custom API


FUTURE DATABASE
    ▲
    │
    └─── Replace in-memory storage with:
         • Firebase Firestore
         • SQLite (sqflite)
         • Hive
         • MongoDB


FUTURE STATE MANAGEMENT
    ▲
    │
    └─── Wrap AuthProvider with:
         • Provider package (MultiProvider)
         • Riverpod
         • GetX
         • Bloc


FUTURE SECURITY
    ▲
    │
    └─── Add:
         • JWT token handling
         • Secure storage
         • Biometric auth
         • Encryption
```

---

**Legend**:
- ✅ Implemented & Ready
- ⚠️ Ready for Extension  
- 🔌 Integration Points
- 📦 Components
- 🔄 Data Flow
